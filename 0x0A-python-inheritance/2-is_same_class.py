#!/usr/bin/python3
"""
Contains one module
"""


def is_same_class(obj, a_class):
    """
    reutrns True if it's an obj of the class
    """
    if type(obj) is a_class:
        return True
    return False
